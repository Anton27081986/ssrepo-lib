import { Colors } from '../../../shared/models';

export interface ButtonColors {
    default: Colors,
    hover: Colors,
    pressed: Colors,
    focused: Colors,
    disabled: Colors,
    disabledIconOnly: Colors
}