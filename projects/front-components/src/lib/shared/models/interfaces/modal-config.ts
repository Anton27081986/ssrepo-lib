import { Orientation } from '../enums';

export interface IModalConfig {
    headerOrientation?: Orientation;
    showHeaderPadding?: boolean;
    showHeaderDivider?: boolean;
    showFooterDivider?: boolean;
}
